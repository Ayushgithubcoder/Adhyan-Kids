# Environment Variables — copy to .env and fill in real values

NODE_ENV=development
PORT=3000

# Cosmos DB — test-04-dev-cosmos
COSMOS_CONNECTION_STRING=AccountEndpoint=https://test-04-dev-cosmos.documents.azure.com:443/;AccountKey=your-key

# Azure credentials (set by CI/CD — do not commit real values)
AZURE_SUBSCRIPTION_ID=your-subscription-id
AZURE_RESOURCE_GROUP=your-resource-group

# Integration templates (auto-added)
APPLICATIONINSIGHTS_CONNECTION_STRING=
